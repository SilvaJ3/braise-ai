// Fonctionnalités en proto, cachées de l'UI tant qu'elles ne sont pas prêtes pour tout le
// monde. Le code reste en place (routes, edge functions, migrations) — seul l'accès depuis
// le menu est coupé.

// V2.5 : publication Instagram. Caché tant que l'app Meta reste en mode dev (testeur unique,
// pas d'App Review soumise) — voir ROADMAP.md.
export const INSTAGRAM_ENABLED = false
